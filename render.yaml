services:
  - type: web
    name: research-navi-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m waitress --listen=0.0.0.0:$PORT main:app
    healthCheckPath: /healthz
    autoDeploy: true
